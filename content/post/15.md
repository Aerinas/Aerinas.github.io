+++
date = '2025-10-04T16:20:59+10:00'
draft = false
title = '15. 三数之和'
description = '排序后固定一个数，使用双指针寻找并去重三元组。'
categories = ['LeetCode Hot 100']
tags = ['LeetCode', '双指针', '排序', '数组']
difficulty = '中等'
leetcode = 15
ai_assisted = true
solution_source = '标准 C++17 实现；生成式 AI 辅助整理'
review_status = '标准实现，后续可替换为个人提交'
+++

> 🤖 **内容说明：** 本文由生成式 AI 辅助整理，代码采用常见标准 C++17 实现，方便先完成知识归档；后续可替换为个人提交版本。

## 🔹 题目

找出数组中所有和为零且不重复的三元组。

## 🔹 思路

- 排序后枚举第一个数，在右侧区间用相向双指针寻找另外两个数。
- 和偏小就移动左指针，偏大就移动右指针。
- 外层元素和命中后的左右元素都要跳过重复值。

## 🔹 C++17

```cpp
class Solution {
public:
    vector<vector<int>> threeSum(vector<int>& nums) {
        vector<vector<int>> answer;
        sort(nums.begin(), nums.end());

        for (int i = 0; i + 2 < static_cast<int>(nums.size()); ++i) {
            if (nums[i] > 0) break;
            if (i > 0 && nums[i] == nums[i - 1]) continue;

            int left = i + 1;
            int right = static_cast<int>(nums.size()) - 1;
            while (left < right) {
                long long sum = static_cast<long long>(nums[i])
                              + nums[left] + nums[right];
                if (sum < 0) {
                    ++left;
                } else if (sum > 0) {
                    --right;
                } else {
                    answer.push_back({nums[i], nums[left], nums[right]});
                    ++left;
                    --right;
                    while (left < right && nums[left] == nums[left - 1]) ++left;
                    while (left < right && nums[right] == nums[right + 1]) --right;
                }
            }
        }
        return answer;
    }
};
```

## 🔹 复杂度

- 时间：`O(n²)`
- 空间：`O(log n)`，不计答案，取决于排序实现

## 🔹 易错点

- 去重是本题核心，外层和内层都不能遗漏。
- 三数求和使用 `long long`，避免整数溢出。
