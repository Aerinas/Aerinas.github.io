+++
title = '239. 滑动窗口最大值'
date = 2026-08-28T00:02:00+10:00
draft = false
description = '使用维护单调递减下标的双端队列求滑动窗口最大值。'
categories = ['LeetCode Hot 100']
tags = ['LeetCode', '单调队列', '滑动窗口', '子串']
difficulty = '困难'
leetcode = 239
ai_assisted = true
solution_source = '标准 C++17 实现；生成式 AI 辅助整理'
review_status = '标准实现，后续可替换为个人提交'
+++

> 🤖 **内容说明：** 本文由生成式 AI 辅助整理，代码采用常见标准 C++17 实现，方便先完成知识归档；后续可替换为个人提交版本。

## 🔹 题目

给定数组和窗口大小 `k`，窗口从左向右移动，返回每个窗口中的最大值。

## 🔹 思路

双端队列保存仍可能成为最大值的元素下标，并让对应数值保持单调递减。队首始终是当前窗口最大值的位置。

## 🔹 C++17

```cpp
class Solution {
public:
    vector<int> maxSlidingWindow(vector<int>& nums, int k) {
        vector<int> answer;
        if (nums.empty() || k <= 0) return answer;

        deque<int> window;
        for (int right = 0; right < static_cast<int>(nums.size()); ++right) {
            while (!window.empty() && window.front() <= right - k) {
                window.pop_front();
            }
            while (!window.empty() && nums[window.back()] <= nums[right]) {
                window.pop_back();
            }
            window.push_back(right);

            if (right >= k - 1) answer.push_back(nums[window.front()]);
        }
        return answer;
    }
};
```

## 🔹 复杂度

- 时间复杂度：`O(n)`，每个下标至多入队、出队各一次。
- 空间复杂度：`O(k)`。

## 🔹 易错点

- 队列存下标而不是只存数值，才能判断元素是否过期。
- 过期条件是 `index <= right - k`。
- 使用 `<=` 删除队尾相等元素，可保留更新的下标。
