+++
title = '3. 无重复字符的最长子串'
date = 2026-08-28T00:00:00+10:00
draft = false
description = '使用滑动窗口和字符最后出现位置求无重复字符的最长子串。'
categories = ['LeetCode Hot 100']
tags = ['LeetCode', '滑动窗口', '哈希表']
difficulty = '中等'
leetcode = 3
ai_assisted = true
solution_source = '标准 C++17 实现；生成式 AI 辅助整理'
review_status = '标准实现，后续可替换为个人提交'
+++

> 🤖 **内容说明：** 本文由生成式 AI 辅助整理，代码采用常见标准 C++17 实现，方便先完成知识归档；后续可替换为个人提交版本。

## 🔹 题目

给定字符串 `s`，求不包含重复字符的最长连续子串长度。空字符串的答案为 0。

## 🔹 思路

用左边界 `left` 和右边界 `right` 表示当前无重复窗口，并记录每个字符最近一次出现的位置。遇到重复字符时，左边界只能向右移动，不能回退。

## 🔹 C++17

```cpp
class Solution {
public:
    int lengthOfLongestSubstring(string s) {
        vector<int> last(256, -1);
        int left = 0;
        int answer = 0;

        for (int right = 0; right < static_cast<int>(s.size()); ++right) {
            unsigned char ch = static_cast<unsigned char>(s[right]);
            left = max(left, last[ch] + 1);
            last[ch] = right;
            answer = max(answer, right - left + 1);
        }
        return answer;
    }
};
```

## 🔹 复杂度

- 时间复杂度：`O(n)`。
- 空间复杂度：`O(1)`，字符表大小固定。

## 🔹 易错点

- 左边界必须使用 `max(left, last[ch] + 1)`，否则可能回退。
- 更新答案时窗口长度为 `right - left + 1`。
- 使用字符作为数组下标时先转为 `unsigned char` 更稳妥。
