+++
title = '141. 环形链表'
date = 2026-08-28T00:06:00+10:00
draft = false
description = '使用 Floyd 快慢指针判断链表是否存在环。'
categories = ['LeetCode Hot 100']
tags = ['LeetCode', '链表', '快慢指针']
difficulty = '简单'
leetcode = 141
ai_assisted = true
solution_source = '标准 C++17 实现；生成式 AI 辅助整理'
review_status = '标准实现，后续可替换为个人提交'
+++

> 🤖 **内容说明：** 本文由生成式 AI 辅助整理，代码采用常见标准 C++17 实现，方便先完成知识归档；后续可替换为个人提交版本。

## 🔹 题目

判断单链表中是否存在某个节点能够沿 `next` 再次回到自身所在路径。

## 🔹 思路

慢指针每次走一步，快指针每次走两步。若有环，快指针一定会在环内追上慢指针；若无环，快指针会先到达末尾。

## 🔹 C++17

```cpp
class Solution {
public:
    bool hasCycle(ListNode* head) {
        ListNode* slow = head;
        ListNode* fast = head;

        while (fast && fast->next) {
            slow = slow->next;
            fast = fast->next->next;
            if (slow == fast) return true;
        }
        return false;
    }
};
```

## 🔹 复杂度

- 时间复杂度：`O(n)`。
- 空间复杂度：`O(1)`。

## 🔹 易错点

- 循环条件必须同时检查 `fast` 和 `fast->next`。
- 相遇判断比较节点指针，而不是节点值。
