+++
title = '141. 环形链表'
date = 2026-08-28T00:06:00+10:00
draft = false
description = '使用 Floyd 快慢指针判断链表是否存在环。'
categories = ['LeetCode Hot 100']
tags = ['LeetCode', '链表', '快慢指针']
difficulty = '简单'
leetcode = 141
+++

## 题目概括

判断单链表中是否存在某个节点能够沿 `next` 再次回到自身所在路径。

## 核心思路

慢指针每次走一步，快指针每次走两步。若有环，快指针一定会在环内追上慢指针；若无环，快指针会先到达末尾。

## 算法步骤

1. 快慢指针都从头节点出发。
2. 在 `fast` 和 `fast->next` 存在时循环。
3. 慢指针走一步，快指针走两步。
4. 若两者相遇返回 `true`，循环结束则返回 `false`。

## C++17 实现

```cpp
class Solution {
public:
    bool hasCycle(ListNode* head) {
        ListNode* slow = head;
        ListNode* fast = head;

        while (fast && fast->next) {
            slow = slow->next;
            fast = fast->next->next;
            if (slow == fast) return true;
        }
        return false;
    }
};
```

## 复杂度分析

- 时间复杂度：`O(n)`。
- 空间复杂度：`O(1)`。

## 易错点

- 循环条件必须同时检查 `fast` 和 `fast->next`。
- 相遇判断比较节点指针，而不是节点值。

## 模板总结

Floyd 快慢指针适用于链式结构中的环检测，核心是利用不同速度制造相对位移。
