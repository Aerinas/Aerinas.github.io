+++
date = '2025-09-29T22:00:00+10:00'
draft = false
title = '49. 字母异位词分组'
description = '使用排序后的字符串作为哈希键，将字母异位词归入同一组。'
categories = ['LeetCode Hot 100']
tags = ['LeetCode', '哈希表', '字符串', '排序']
difficulty = '中等'
leetcode = 49
ai_assisted = true
solution_source = '标准 C++17 实现；生成式 AI 辅助整理'
review_status = '标准实现，后续可替换为个人提交'
+++

> 🤖 **内容说明：** 本文由生成式 AI 辅助整理，代码采用常见标准 C++17 实现，方便先完成知识归档；后续可替换为个人提交版本。

## 🔹 题目

把由相同字符组成、排列顺序不同的字符串归入同一组。

## 🔹 思路

- 复制每个字符串并排序，排序结果作为哈希键。
- 字母异位词排序后相同，因此会进入同一个桶。
- 最后收集哈希表里的所有桶。

## 🔹 C++17

```cpp
class Solution {
public:
    vector<vector<string>> groupAnagrams(vector<string>& strs) {
        unordered_map<string, vector<string>> groups;
        for (const string& word : strs) {
            string key = word;
            sort(key.begin(), key.end());
            groups[key].push_back(word);
        }

        vector<vector<string>> answer;
        answer.reserve(groups.size());
        for (auto& [key, words] : groups) {
            answer.push_back(move(words));
        }
        return answer;
    }
};
```

## 🔹 复杂度

- 时间：`O(n · L log L)`，`L` 为字符串平均长度
- 空间：`O(n · L)`

## 🔹 易错点

- 排序键的副本，不能破坏需要加入结果的原字符串。
- `unordered_map` 遍历顺序不固定，但本题不要求答案顺序。
