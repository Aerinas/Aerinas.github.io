+++
title = '53. 最大子数组和'
date = 2026-08-28T00:03:00+10:00
draft = false
description = '使用 Kadane 动态规划求连续子数组的最大和。'
categories = ['LeetCode Hot 100']
tags = ['LeetCode', '动态规划', '数组']
difficulty = '中等'
leetcode = 53
+++

## 题目概括

给定整数数组，寻找和最大的连续非空子数组并返回其元素和。

## 核心思路

`current` 表示必须以当前位置结尾的最大子数组和。对于当前数，要么接在前一段后面，要么从当前数重新开始。

## 算法步骤

1. 空数组按防御性处理返回 0。
2. 用首元素初始化 `current` 和 `answer`。
3. 对每个后续元素计算 `current = max(nums[i], current + nums[i])`。
4. 用 `current` 更新全局最大值。

## C++17 实现

```cpp
class Solution {
public:
    int maxSubArray(vector<int>& nums) {
        if (nums.empty()) return 0;

        int current = nums[0];
        int answer = nums[0];
        for (int i = 1; i < static_cast<int>(nums.size()); ++i) {
            current = max(nums[i], current + nums[i]);
            answer = max(answer, current);
        }
        return answer;
    }
};
```

## 复杂度分析

- 时间复杂度：`O(n)`。
- 空间复杂度：`O(1)`。

## 易错点

- 子数组必须非空，不能把初值设为 0，否则全负数组会出错。
- `current` 与 `answer` 的含义不同：一个必须以当前位置结尾，一个是全局最优。

## 模板总结

当连续选择可以在每个位置“延续旧状态或重新开始”时，可使用 Kadane 模板压缩一维动态规划。
