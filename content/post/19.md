+++
title = '19. 删除链表的倒数第 N 个结点'
date = 2026-08-28T00:10:00+10:00
draft = false
description = '使用哑节点和固定间距双指针一次遍历删除倒数第 N 个节点。'
categories = ['LeetCode Hot 100']
tags = ['LeetCode', '链表', '双指针']
difficulty = '中等'
leetcode = 19
ai_assisted = true
solution_source = '标准 C++17 实现；生成式 AI 辅助整理'
review_status = '标准实现，后续可替换为个人提交'
+++

> 🤖 **内容说明：** 本文由生成式 AI 辅助整理，代码采用常见标准 C++17 实现，方便先完成知识归档；后续可替换为个人提交版本。

## 🔹 题目

删除单链表倒数第 `n` 个节点并返回头节点。题目保证 `n` 合法。

## 🔹 思路

让 `fast` 比 `slow` 提前 `n + 1` 步，使 `fast` 到达末尾时，`slow` 正好停在待删除节点的前驱。哑节点让删除头节点也使用相同逻辑。

## 🔹 C++17

```cpp
class Solution {
public:
    ListNode* removeNthFromEnd(ListNode* head, int n) {
        ListNode dummy(0, head);
        ListNode* fast = &dummy;
        ListNode* slow = &dummy;

        for (int i = 0; i <= n; ++i) {
            if (!fast) return head;
            fast = fast->next;
        }
        while (fast) {
            fast = fast->next;
            slow = slow->next;
        }

        ListNode* removed = slow->next;
        slow->next = removed->next;
        delete removed;
        return dummy.next;
    }
};
```

## 🔹 复杂度

- 时间复杂度：`O(L)`。
- 空间复杂度：`O(1)`。

## 🔹 易错点

- 为定位前驱，快指针需要比慢指针多走 `n + 1` 步。
- 删除头节点时必须借助哑节点。
- LeetCode 保证 `n` 合法；防御性判断用于避免非法输入解引用空指针。
